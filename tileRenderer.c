#include <utils.h>
#include <stdio.h>
#include <stdlib.h>

void renderGrid(grid g)
{
    for(int y = 0; y < MAX_MAP_HEIGHT; y++)
    {
        for(int x = 0; x < MAX_MAP_WIDTH; x++)
        {
            
        }
    }
}